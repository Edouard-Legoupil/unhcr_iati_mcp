# Azure Function App dependencies for UNHCR MCP Server

# Core dependencies (from requirements.txt)
fastmcp>=1.0.0
aiohttp>=3.8.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
tenacity>=8.0.0
uvicorn[standard]>=0.20.0

# Azure Functions specific dependencies
azure-functions>=1.16.0
azure-identity>=1.15.0
azure-keyvault-secrets>=4.5.0

# Required for Starlette ASGI compatibility
starlette>=0.37.0

# Note: mcp package removed to avoid conflicts with fastmcp
# fastmcp already includes all necessary MCP protocol support

# Optional: For better async support
anyio>=4.0.0
httpx>=0.24.0

# Optional: For JSON logging
json-logging>=1.0.0

# Optional: For environment variable management
python-dotenv>=1.0.0
